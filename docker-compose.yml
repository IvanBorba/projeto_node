services:
  api:
    container_name: api
    image: api_express
    build: .
    ports:
      - 8080:8080
    environment:
      - DB_USER=ivan
      - DB_PASSWORD=101112
      - DB_HOST=db
      - DB_PORT=5432
      - DB=defaultdb
    depends_on:
      - db
  db:
    container_name: db
    image: postgres:12
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=ivan
      - POSTGRES_PASSWORD=101112
      - POSTGRES_DB=defaultdb
    volumes:
      - ./create_tables.sql:/docker-entrypoint-initdb.d/create_tables.sql
